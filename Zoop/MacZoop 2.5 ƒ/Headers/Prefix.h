/* Target settings for Carbon project- make this the prefix file */#ifndef TARGET_API_MAC_CARBON#define TARGET_API_MAC_CARBON               1#endif// MacZoop may use this directive to take decisions about the type of target it's// compiling for. Set this as appropriate when you compile. You can (and should)// precompile MacHeaders with TARGET_API_MAC_CARBON set to 1, but the local MacZoop// source code presently needs ot see this flag set appropriately for the target. The// following set up will arrange this.#define     COMPILE_TO_CLASSIC_TARGET       0// You normally shouldn't have to change anything below this line.#include	"MacHeadersCarbon"#if TARGET_API_MAC_CARBON	#include	<MacHelp.h>	// sorry, but changing these compiler macros is just SILLY. Give that back!!!	#undef PRAGMA_ALIGN_SUPPORTED	#define PRAGMA_ALIGN_SUPPORTED  PRAGMA_STRUCT_ALIGN	#undef GENERATINGPOWERPC	#define GENERATINGPOWERPC TARGET_CPU_PPC#endif#if COMPILE_TO_CLASSIC_TARGET  #define COMPILE_TO_CARBON_TARGET          0  #undef  TARGET_API_MAC_CARBON  #define TARGET_API_MAC_CARBON             0#else  #define COMPILE_TO_CARBON_TARGET          1  #endif