/****************************************************************************************************			MacZoop - "the framework for the rest of us"		 ****			MacZoopCarbon.h			-functions to keep things working across Carbon & Classic***			© 2000, Graham Cox*****************************************************************************************************/#pragma once#ifndef __MACZOOPCARBON__#define	__MACZOOPCARBON__#include	<Dialogs.h>/*These functions are needed to be supplied by MaZoop because while they exist as Macros underthe classic programming model, they do not exist at present under the new one.So that we can maintain identical source-code compatibility all the way back to System 7,rather than change the API (which would limit us to 8.5), we simply provide the missingfunctions.In the long run, this may change.*/#if ACCESSOR_CALLS_ARE_FUNCTIONS// these functions do NOT exist in the Carbon API so we provide them using Carbonvoid	GetWindowContentRgn( WindowPtr w, RgnHandle rgn );void	GetWindowStructureRgn( WindowPtr w, RgnHandle rgn );void	GetWindowUpdateRgn( WindowPtr w, RgnHandle rgn );#else// these functions DO exist in the Carbon API but not otherwise, so we provide them#if UNIVERSAL_INTERFACES_VERSION < 0x0335void	SetPortDialogPort( DialogPtr dlg );#endif#endif#endif